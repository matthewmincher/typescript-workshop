import { Species } from "../types/enums";
import { Pet } from "../types/Pet";

let pets: Pet[] = [
  {
    id: 1,
    name: "Spud",
    species: Species.Cat,
    weight: 7.1,
  },
];

export default pets;
